<?php
/*
  Create an (awful) encryption class that will convert 
  each character to binary, move the highest order bit
  (assuming 7 bits per character) to the lowest order, 
  and shift all other bits up one place.

  Only has to work for non-extended ASCII characters.

  Example: "1100101" becomes "1001011"
*/

function encrypt($string) {

}

function decrypt($string) {

}
